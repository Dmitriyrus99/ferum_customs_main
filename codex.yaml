image: universal

setup:
  run: |
    ./setup.sh

tasks:
  - name: Python tests
    run: pytest tests/
  - name: Lint and pre-commit
    run: pre-commit run --all-files || echo "pre-commit not installed"
